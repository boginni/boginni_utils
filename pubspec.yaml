name: boginni_utils
description: Utitilities shared in projects developed by boginni
version: 0.1.0

repository: https://github.com/boginni/boginni_utils

environment:
  sdk: '>=3.0.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: